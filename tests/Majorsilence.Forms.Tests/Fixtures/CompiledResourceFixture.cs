namespace Majorsilence.Forms.Tests.Fixtures;

// Marker type only: its FullName ("Majorsilence.Forms.Tests.Fixtures.CompiledResourceFixture")
// must match the compiled manifest resource name the SDK derives for the sibling
// CompiledResourceFixture.resx ("<RootNamespace>.Fixtures.CompiledResourceFixture.resources"),
// so `new ComponentResourceManager (typeof (CompiledResourceFixture))` finds it.
public sealed class CompiledResourceFixture
{
}
