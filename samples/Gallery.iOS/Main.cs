using UIKit;

namespace Gallery.iOS
{
    public static class MainClass
    {
        private static void Main (string[] args) => UIApplication.Main (args, null, typeof (GalleryAppDelegate));
    }
}
